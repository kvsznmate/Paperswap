# Walkthrough

Moved. The project walkthrough lives at the repository root:
[`../walkthrough.md`](../walkthrough.md).

This copy had drifted badly — it described a SQLite database, a Pillow card
renderer that no longer exists, and a two-topic feed — so it is now a pointer
rather than a second thing to keep in sync.

For backend development specifics (modules, schema, operational gotchas), see
[README.md](README.md) in this directory.
